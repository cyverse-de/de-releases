## Analyses Service Settings
analyses.db.host = {{ groups['dbms_primary'][0] }}
analyses.db.port = {{ pg_listen_port }}
analyses.db.password = {{ dbms_connection_pass }}
analyses.username.suffix = @{{ uid_domain }}
