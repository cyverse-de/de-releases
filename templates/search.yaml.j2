elasticsearch:
  base: "{{ es_base_uri.rtrim(pat="/")}}"
  user: "{{ es_username }}"
  password: "{{ es_password }}"
  index: "{{ es_index }}"
data_info:
  base: "{{ baseurls_data_info.rtrim(pat="/")}}"
